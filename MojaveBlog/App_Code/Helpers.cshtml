@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<MojaveBlog.Core.Tag> tags)
{
foreach (var tag in tags)
{
        <div class="tags-div">
            @MojaveBlog.ActionLinkExtensions.TagLink(htmlHelper, tag)
        </div>
}
}